name: my_custom_lints
description: Custom lint rules for Flutter
version: 0.0.1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  analyzer: ^7.6.0
  analyzer_plugin: ^0.13.4
  custom_lint_builder: ^0.8.0
  flutter:
    sdk: flutter
  glob: ^2.1.3
  path: ^1.9.1
  yaml: ^3.1.3
  collection: ^1.19.1
  flutter_bloc: ^9.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  custom_lint: ^0.7.0
